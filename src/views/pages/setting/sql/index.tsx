/**
 * @fileOverview SQL
 * @date 2023-08-28
 * @author poohlaha
 */
import React, { ReactElement } from 'react'
import { observer } from 'mobx-react-lite'

const Sql = (): ReactElement => {
  const render = () => {
    return <div className="sql-page wh100"></div>
  }

  return render()
}

export default observer(Sql)
